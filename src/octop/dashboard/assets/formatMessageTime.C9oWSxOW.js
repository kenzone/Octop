function resolveMessageTimestampMs(raw){if(typeof raw=="number"&&raw>0)return raw;if(typeof raw=="string"&&raw.trim()){const parsed=Number(raw);if(Number.isFinite(parsed)&&parsed>0)return parsed}return 0}function asDate(input){const d=input instanceof Date?input:new Date(input);return Number.isNaN(d.getTime())?null:d}function formatServerYmd(input,timeZone){const d=asDate(input);return d?new Intl.DateTimeFormat("en-CA",{timeZone,year:"numeric",month:"2-digit",day:"2-digit"}).format(d):""}function timeZoneOptions(timeZone){return timeZone?{timeZone}:{}}function formatServerHourMinute(input,timeZone){var _a,_b;const d=asDate(input);if(!d)return"";const parts=new Intl.DateTimeFormat("en-GB",{timeZone,hour:"2-digit",minute:"2-digit",hour12:!1}).formatToParts(d),hour=((_a=parts.find(p=>p.type==="hour"))==null?void 0:_a.value)??"00",minute=((_b=parts.find(p=>p.type==="minute"))==null?void 0:_b.value)??"00";return`${hour}:${minute}`}function calendarDaysAgo(thenInput,timeZone,now=new Date){const then=asDate(thenInput);if(!then)return Number.NaN;const todayKey=formatServerYmd(now,timeZone),thenKey=formatServerYmd(then,timeZone),[ty,tm,td]=todayKey.split("-").map(Number),[oy,om,od]=thenKey.split("-").map(Number);return Math.floor((Date.UTC(ty,tm-1,td)-Date.UTC(oy,om-1,od))/864e5)}function formatMessageTime(tsMs,timeZone){if(!tsMs||tsMs<=0)return"";const d=new Date(tsMs),now=new Date,tz=timeZoneOptions(timeZone),hhmm=d.toLocaleTimeString(void 0,{hour:"2-digit",minute:"2-digit",...tz});return formatServerYmd(d,timeZone)===formatServerYmd(now,timeZone)?hhmm:`${d.toLocaleDateString(void 0,{month:"2-digit",day:"2-digit",...tz})} ${hhmm}`}const SERVER_DATE_TIME_OPTS={year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit"};function formatServerDateTime(epochSec,timeZone){return epochSec?new Date(epochSec*1e3).toLocaleString(void 0,{...SERVER_DATE_TIME_OPTS,...timeZoneOptions(timeZone)}):"—"}function formatServerIsoDateTime(iso,timeZone){const trimmed=iso.trim();if(!trimmed)return"—";const d=new Date(trimmed);return Number.isNaN(d.getTime())?iso:d.toLocaleString(void 0,{...SERVER_DATE_TIME_OPTS,...timeZoneOptions(timeZone)})}export{formatServerIsoDateTime as a,formatMessageTime as b,calendarDaysAgo as c,formatServerYmd as d,formatServerHourMinute as e,formatServerDateTime as f,resolveMessageTimestampMs as r};
