const PROBE_ON_SAVE_KEY="octop.customMcp.probeOnSave",EXAMPLE_JSON=`{
  "deepwiki": {
    "display_name": "DeepWiki",
    "url": "https://mcp.deepwiki.com/mcp",
    "transport": "streamable_http"
  }
}`;function friendlyServerLabel(card){return(card.displayName??"").trim()||card.name.trim()||"mcp-server"}const MCP_ACCENT_PALETTE=["#0d9488","#2563eb","#7c3aed","#db2777","#ea580c","#0891b2","#65a30d","#d97706","#4f46e5","#e11d48"];function accentForServerName(name){let hash=0;for(let i=0;i<name.length;i+=1)hash=hash*31+name.charCodeAt(i)>>>0;return MCP_ACCENT_PALETTE[hash%MCP_ACCENT_PALETTE.length]}function headersToText(headers){return headers?Object.entries(headers).map(([k,v])=>`${k}: ${v}`).join(`
`):""}function parseHeadersText(text){const out={};for(const line of text.split(`
`)){const trimmed=line.trim();if(!trimmed)continue;const idx=trimmed.indexOf(":");if(idx<=0)throw new Error(`invalid header line: ${trimmed}`);const key=trimmed.slice(0,idx).trim(),value=trimmed.slice(idx+1).trim();if(!key)throw new Error(`invalid header line: ${trimmed}`);out[key]=value}return out}function envToText(env){return env?Object.entries(env).map(([k,v])=>`${k}=${v}`).join(`
`):""}function parseEnvText(text){const out={};for(const line of text.split(`
`)){const trimmed=line.trim();if(!trimmed)continue;const idx=trimmed.indexOf("=");if(idx<=0)throw new Error(`invalid env line: ${trimmed}`);const key=trimmed.slice(0,idx).trim(),value=trimmed.slice(idx+1);if(!key)throw new Error(`invalid env line: ${trimmed}`);out[key]=value}return out}function serversToCards(servers){return Object.entries(servers).map(([name,spec],index)=>{var _a,_b;return{key:`${name}-${index}`,name,displayName:(spec.display_name??"").trim(),transport:spec.transport==="stdio"?"stdio":"streamable_http",url:spec.url??"",headersText:headersToText(spec.headers),command:spec.command??"",argsText:(spec.args??[]).join(`
`),envText:envToText(spec.env),enabled:spec.enabled!==!1,defaultOpen:spec.default_open===!0,shared:spec.shared===!0,collapsed:!0,oauthConfigured:((_a=spec.oauth)==null?void 0:_a.configured)===!0,oauthExpiresAt:(_b=spec.oauth)==null?void 0:_b.expires_at}})}function cardsToServers(cards){const servers={};for(const card of cards){const name=card.name.trim();if(!name)throw new Error("empty_name");if(servers[name])throw new Error("duplicate_name");const spec={transport:card.transport,enabled:card.enabled},displayName=card.displayName.trim();if(displayName&&(spec.display_name=displayName),card.defaultOpen&&(spec.default_open=!0),card.shared&&(spec.shared=!0),card.transport==="streamable_http"){spec.url=card.url.trim();const headers=parseHeadersText(card.headersText);Object.keys(headers).length>0&&(spec.headers=headers)}else{spec.command=card.command.trim();const args=card.argsText.split(`
`).map(line=>line.trim()).filter(Boolean);args.length>0&&(spec.args=args);const env=parseEnvText(card.envText);Object.keys(env).length>0&&(spec.env=env)}servers[name]=spec}return servers}function hasHttpProbeTargets(cards){return Array.isArray(cards)?cards.some(card=>card.transport==="streamable_http"):Object.values(cards).some(spec=>spec&&typeof spec=="object"&&spec.transport!=="stdio")}function mergeCustomMcpCards(servers,prevCards){return serversToCards(servers).map(card=>{const prev=prevCards.find(c=>c.name.trim()===card.name.trim());return prev?{...card,key:prev.key,collapsed:prev.collapsed}:card})}function oauthHintsFromServers(servers,cards){var _a,_b;const hints={};for(const card of cards){const spec=servers[card.name.trim()];card.transport==="streamable_http"&&((_a=spec==null?void 0:spec.oauth)!=null&&_a.required)&&((_b=spec.oauth)==null?void 0:_b.configured)!==!0&&(hints[card.key]=!0)}return hints}function newCard(transport,index){const base=transport==="stdio"?"stdio-server":"http-server",name=`${base}-${index+1}`;return{key:`${base}-${Date.now()}-${index}`,name,displayName:"",transport,url:"",headersText:"",command:"",argsText:"",envText:"",enabled:!0,defaultOpen:!1,shared:!1,collapsed:!1,oauthConfigured:!1}}const CONNECTORS_CHANGED_EVENT="octop:connectors-changed";function notifyConnectorsChanged(){window.dispatchEvent(new CustomEvent(CONNECTORS_CHANGED_EVENT))}export{CONNECTORS_CHANGED_EVENT as C,EXAMPLE_JSON as E,PROBE_ON_SAVE_KEY as P,accentForServerName as a,newCard as b,cardsToServers as c,friendlyServerLabel as f,hasHttpProbeTargets as h,mergeCustomMcpCards as m,notifyConnectorsChanged as n,oauthHintsFromServers as o,serversToCards as s};
